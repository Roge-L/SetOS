{
  "name": "setos",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "SetOS — a personal calorie/macro + workout tracker you talk to through Claude over MCP.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "inspector": "npx @modelcontextprotocol/inspector"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.8.2",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@supabase/supabase-js": "^2.102.1",
    "agents": "^0.17.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "@types/node": "^22.15.2",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4",
    "wrangler": "~4.77.0"
  }
}
